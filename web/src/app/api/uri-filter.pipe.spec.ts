import { UriFilterPipe } from './uri-filter.pipe';

describe('UriFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UriFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
